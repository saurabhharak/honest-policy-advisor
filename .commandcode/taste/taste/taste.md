# Taste
- Prefers test-first / TDD: write test cases before implementing a new feature. Confidence: 0.95
- Opens architecture discussions with an explicit brainstorm request ("let's brain strom it") before any building — expects the agent to first inspect the current codebase (e.g., read the parser/extractor to ground the discussion) and present honest options with tradeoffs; the user then contributes their own novel architectural ideas rather than merely picking from the offered menu (e.g., when asked how to store guidelines and extract from 20+ page documents, proposed a page-by-page "red flag triage" agent instead of choosing among the Python/JSON/RAG options). Confidence: 0.6
- Proposed (and expects to build toward) a page-by-page "red flag triage" agent for 20+ page policy documents: every page is read and judged so nothing in the middle of a long doc can be missed, concerning findings accumulate across pages in graph state/memory, and a separate second LLM writes the plain-language verdict from the accumulated findings + deterministic calculator outputs (never raw pages, never doing math) — a two-model split where the triage LLM does judgment and the layman writer only rephrases. Confidence: 0.65
- When choosing architecture components (e.g., agent framework, memory layer), wants side-by-side comparative research of competing open-source candidates the user names — explicitly asked to compare LangGraph vs Deep Agents "for agents" and Honcho vs TencentDB Agent Memory "for memory" — with honest tradeoffs (abstraction level, license/copyleft, deployment weight, fit for the project's actual use case) feeding the recommendation, before any design doc or code is written. Confidence: 0.6
- After side-by-side research of candidate options, wants a single decisive verdict rather than a menu or hedge: explicitly asked "now tell me which option we should go with?" for the agent-framework and memory choices — the deliverable after research is one committed recommendation (with the reasoning that justifies it) the user can green-light, not a set of options to choose from. Once the verdict is given, the user commits to it decisively and immediately ("lets go with the langraph for agent as well memeory") rather than re-opening the debate. Confidence: 0.7
- Wants all project documentation (README, ENGINEERING, AGENTS, CONTRIBUTING, etc.) updated to industry standards and kept in sync with the actual codebase architecture — explicitly asked to "update all the docs and readme according to the industry standards" after the docs had drifted (describing an obsolete linear pipeline and referencing a non-existent module). "Industry standards" here means: README with architecture overview, setup, config table, usage, security/observability; engineering rules with single-responsibility tables and anti-patterns; accurate agent-context file; contributing workflow consistent with CI gates. Confidence: 0.7
- When setting up a new remote repo, give it a meaningful name and a description at the repo level, and update README.md (and AGENTS.md) to match the project's actual scope. Confidence: 0.7
- For MCP server config changes, verify the exact configured command end-to-end before considering it fixed — spawn the server, send a proper `initialize` handshake over stdio, and confirm the running version/handshake response. Confidence: 0.6
- Use the `gh` CLI for GitHub operations (creating remote repos, pushing); user expects it to be authenticated and available. Confidence: 0.8
- Shell is Windows cmd.exe: do not chain commands with `;` and do not use heredocs (`<<'EOF'`); run commands separately and write multi-line content (e.g., commit messages) to a temp file, then reference it (e.g., `git commit -F <file>`). Confidence: 0.9
- Windows console default encoding is cp1252, which cannot print the ₹ character in this domain's output; scripts that print analysis/verdict text must force UTF-8 stdout (e.g., `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`) or the run crashes with UnicodeEncodeError. Confidence: 0.6
- On Windows cmd.exe, avoid `<`/`>` in command arguments — they are parsed as redirects and silently break commands (e.g., `--with mcp<2` fails with "cannot find file '2'"); prefer exact version pins with `==` (e.g., `mcp==1.29.0`). Confidence: 0.8
- Prefers using MCP tools (e.g., the fetch MCP server) for web content retrieval over shell-based scraping/curl — explicitly requests tasks be done "using the mcp". Confidence: 0.6
- When asked how to build/approach a feature, wants the answer grounded in researched industry best practices and authoritative sources (regulator/IRDAI rules, advisor frameworks) — explicitly says "check web and see what are the best industry practices companies are using" rather than relying on the agent's own opinion. Repeated again when choosing security tooling: "check on web and tell the opensource project on github". Repeated again for domain questions: when asked who professionally reviews policies and how it's done manually, the user said "do some web reserch and tell me how it is done manually" — expects real-world/practitioner-grounded answers (roles, actual checklist, manual workflow, regulatory references) with cited sources, not opinion. Repeated again when asking what to check when receiving a policy: "are there any checklist or gold standards or there are guidelines" — expects authoritative checklists (IRDAI regulations, independent advisor checklists like Ditto's) to drive what the product flags per policy type. Confidence: 0.9
- Wants the product to flag user-impacting policy terms and explain them in plain language for lay users: the tool's value proposition is that "normal people will not read and they will not understand the way the policy has written" — so anything that affects the user (co-pay, room-rent caps, charges, waiting periods, free-look window) should be surfaced as a flag with the term explained in ordinary words, per policy type, not just reported as a raw clause. Confidence: 0.7
- Grounds the product's per-policy-type checklists in authoritative gold standards: when asked what to check when receiving a policy ("are there any checklist or gold standards or there are guidelines"), the expected answer maps each check to a named source — Ditto's health checklist (must-haves per flag), IRDAI Regulations 2024 legal minimums (30-day free-look, charge disclosure), and the advisor evaluation framework (XIRR, surrender ratio) — rather than an ad hoc list. Confidence: 0.6
- Wants new tooling/frameworks (e.g., observability) validated before adoption: asked to "do some research and check do we need an observability framework if yes where and why" — expects a needs assessment plus a complete plan justifying placement and rationale, rather than adding infrastructure on trend alone. Repeated this pattern when evaluating Opik ("do we need an observability framework... is this a good framework"), and expects an honest cost/benefit answer (e.g., "premature for a hackathon; structured logging first") rather than a yes-plus-install. Repeated again for Docling ("check dockling and let me know how feasible it is to add it to our project read the latest documentation"), where the expected deliverable was a feasibility assessment — current version, dependency/hardware weight, concrete integration point in the existing architecture, honest tradeoffs, recommendation — before any code is written. Confidence: 0.9
- Prefers scoping down to one domain (e.g., health insurance) first and expanding to more domains incrementally later, rather than building breadth all at once. Confidence: 0.8
- Cares about the reliability of proposed data/metrics — asks how trustworthy the underlying data is and expects guardrails (multi-year trends, data vintage, source URLs, flagging missing data) over single-number rankings. Confidence: 0.6
- Wants data stored in a structured, comparison-friendly way (reference tables + per-review records) so policies can be compared against each other later. Confidence: 0.55
- Prefers deterministic math (ratios, XIRR, surrender-value formulas) computed in pure Python code, never by the LLM — the LLM only writes narrative/verdict from pre-computed facts so results can't hallucinate. Confidence: 0.8
- Prefers async-safe library integration: when adopting a framework built on asyncio (e.g., NeMo Guardrails), wants explicit async variants plus sync facades that detect a running event loop, to avoid deadlocks in async hosts. Confidence: 0.6
- Wants framework configuration done explicitly and correctly: library config must define the actual flow definitions the framework requires (e.g., Colang flows via `RailsConfig.from_content()`), not just a system prompt — flagged a missing-config gap. Confidence: 0.6
- Wants heavy/optional third-party dependencies isolated in optional dependency groups (e.g., `[project.optional-dependencies] guardrails`) with lazy imports, so the core app runs and tests pass without the package installed. Confidence: 0.6
- Prefers tests to mock the heavy external library (e.g., mocked NeMo rails) so the test suite never makes real LLM calls or needs the optional package installed. Confidence: 0.55
- Wants domain outputs (complaint letters, policy analysis) to cite exact regulatory references — circular numbers, regulation names, mandated timelines — for credibility, rather than generic language. Confidence: 0.7
- Prefers routing documents by type into separate Pydantic schemas + calculators (a "traffic cop" classifier at the front of the pipeline) over one generic schema, to avoid hallucinated extraction and crashes. Confidence: 0.65
- Likes multi-agent "red teaming" for important generated documents (a drafting agent plus a compliance/review agent that hardens citations). Confidence: 0.55
- Prefers bundling curated reference data (e.g., insurer metrics) as versioned JSON with source URLs and a refresh script over runtime-only fetching. Confidence: 0.6
- Cares that the project repo conforms to industry standards for agent repos — expects the professional scaffolding (CI/CD workflow, actual LICENSE file, type checking, pre-commit hooks, CONTRIBUTING/CODE_OF_CONDUCT) and an honest gap assessment against those standards rather than a rosy "all good" answer. Confidence: 0.55
- Wants release/versioning engineering to follow open-source industry standards: asked "how the versioning is done in opensource project and how we should do it what are the industry follows and standards" and then confirmed "i want versioning up to the standards" — SemVer (PEP 440) incl. the 0.x pre-stability rule, Conventional Commits feeding automated version bumps (python-semantic-release/Commitizen), annotated git tags, a Keep-a-Changelog CHANGELOG.md, and a single source of truth for the version (pyproject.toml read via importlib.metadata, not a duplicated `__version__` string). Confidence: 0.8
- Plans releases as scoped milestones tied to architecture phases: explicitly froze the current state as v0.1.0 (immutable hackathon baseline) and scoped v0.2.0 to the upcoming agent-framework + state-management work — expects a documented release roadmap (VERSIONING.md) and a tagged, reproducible baseline before starting a major migration. Confidence: 0.65
- Release hygiene when cutting a tag with uncommitted fixes in the working tree: prefers committing the real bug fixes INTO the release so the frozen tag is clean and fully working (chose "commit fixes into v0.1.0" over tagging the buggy HEAD), and committing the versioning docs (CHANGELOG, VERSIONING policy, CONTRIBUTING section) along with the release so the tag is meaningful — with the full quality gates (tests, ruff, mypy) run green before tagging. Confidence: 0.7
- Wants a git commit after each major functionality/unit of work — explicitly asked to "commit the code after each major functionality"; expects atomic, focused commits that keep history clean (e.g., separate commits per feature, tooling change, CI, docs) rather than one large commit at the end. Confidence: 0.9
- Expects completed work to be committed AND pushed to the remote — asked to "commit and push the changes" and expects the standard workflow to end with the commits on the remote (with CI running there), not just local commits. Confidence: 0.6
- Wants LLM interactions wrapped in safety guardrails (input rails for prompt-injection protection, output rails for schema/tone validation) for agents that ingest untrusted user content — proactively asked where NeMo Guardrails could be integrated into the LLM pipeline. Confidence: 0.55
- Prefers error handling that raises typed exceptions (e.g., a custom validation error carrying a reason + user-facing message) rather than returning sentinel/placeholder strings — explicitly flagged a sentinel-string design as a crash gap because blocked content would flow downstream and break deserialization. Confidence: 0.7
- Cost/latency-conscious about LLM-based checks: never run LLM guardrail/validation calls over large payloads (full JSON blobs, multi-thousand-token documents); sample only high-risk free-text fields and cap the input (e.g., ~2k chars) to avoid latency spikes and context-window overruns. Confidence: 0.65
- Wants deterministic validation for structured data: JSON outputs should be schema-checked by Pydantic (fast, no LLM round-trip), and LLM-based rails reserved only for natural-language text boundaries (letters, replies) — opposes redundant LLM calls to validate JSON shape. Confidence: 0.7
- Wants the LLM→calculator interface to be an explicit typed field contract: the design must specify exactly what info the model must return for the deterministic calculations and in what format (field keys, types, examples, and which calculator each field feeds), with the model's JSON output validated by Pydantic at both boundaries (per-page triage output and the final merged schema) — the LLM supplies values, Python does all arithmetic. Confidence: 0.8
- Prefers a complete written plan with rationale before implementation, with scope decisions explicitly surfaced (e.g., guard at one input boundary vs. multiple functions) and expects to review the plan and provide their own thoughts before work proceeds; when the user returns detailed technical critique, expects the plan to be revised and re-presented incorporating every point, not pushed back on. Directly approved moving ahead with a multi-agent refactor plan with "yes create a plan". Confidence: 0.95
- Wants optional integrations verified live end-to-end against the real endpoints, not left mock-tested: when the agent flags a caveat that a library "hasn't been exercised end-to-end", the user asks to enable it (e.g., add the flags to the gitignored .env) and "do the live testing" to resolve the caveat rather than deferring it — mocks are fine for the test suite, but the real integration must be proven live. Repeatedly confirms this: after the agent noted Opik wasn't yet run against a real instance, the user immediately said "setup the local docker and run it live", standing up a full local Docker stack and demanding real traces verified in the UI. Most recently, after the multi-agent refactor shipped with a caveat that the pipeline was only mock-tested, the user's next message was "test using this policy end to end" with a real policy PDF path — the full pipeline must be proven against real user documents, not just unit tests; in the latest exchange the user again personally handed over a real policy PDF ("use this policy for testing") to drive the end-to-end validation of the new Docling pipeline. When the agent offered "wire + run the live agent on Telegram/email through the supervisor" as the highest-value next step vs. "README/architecture docs first", the user replied "will go with the 1" — the deployed-channel live proof takes priority over polish; most recently the user again asked the agent to start the live bot ("run the agent for me i will send a real policy pdf using telegram") so they could personally drive the end-to-end test by sending a real policy PDF through the real Telegram channel while the agent monitored traces. Most recently, right after the LangGraph migration shipped, the user again handed over a real policy PDF path ("now do the live test using this ... .pdf file") to drive a live run of the compiled graph — the agent wrote a live-test script, ran it against real Docling + real LLM, and fixed the bugs it surfaced (extraction windowing gap, memory model fallback). Confidence: 0.99
- When deciding whether to adopt infrastructure (e.g., observability), wants the decision weighed against the project's future trajectory — production users, multi-turn agents, evaluation — not just its current hackathon stage; explicitly prefers adopting foundational pieces now when retrofitting later would be harder ("now we can add later it might be a bit difficult... think in this way that this system is going to handle production users real soon... this project will go beyond this hackathon"). Confidence: 0.8
- Prefers infra/environment configuration to be enabled in the gitignored local `.env` (not hardcoded or committed): live testing is done by flipping `OPIK_ENABLED=true` and `OPIK_URL` in `.env`, while the committed `.env.example` documents the vars — local config stays local, secrets never land in git. Reconfirmed when wiring a new live Telegram bot: user handed over the new @BotFather token and said "add the things on .env and wire it so i can test with real data" — expects the token swapped into local `.env` and the live agent started and verified, with the wiring committed but the secret staying local. Confidence: 0.75
- Values single-responsibility modular design: asks how many components can be carved into "individual agents" with clean boundaries (LLM-judgment agents vs. deterministic tools), signaling interest in a supervisor + specialist agent architecture where pure-math calculators stay as tools rather than agents. Confidence: 0.4
- Prefers a fully agentic architecture over a conservative one: when the agent recommended keeping the extractor as a one-shot tool, the user pushed back ("why can't we add extractor as a agent"), so anything exercising LLM judgment (vision extraction included) should become a proper agent with a validate-and-retry loop, not a thin tool call. Confidence: 0.6
- Wants a dedicated Researcher agent that fetches on-demand web/market information (via the fetch MCP server) to ground analyst verdicts in current real-world data — explicitly said "we should need a researcher which will fetch the information as required" — complementing static bundled datasets rather than relying on them alone. Confidence: 0.65
- When the agent flags a residual caveat (e.g., new agents only mock-tested, real fetch path unproven), wants that caveat resolved with a live end-to-end smoke test (send a real message through the supervisor with local Opik up and watch parallel spans land in the trace) rather than left as a known gap. Confidence: 0.6
- When setting up third-party infrastructure (e.g., Opik self-hosted), follows the vendor's official deployment instructions (official docker-compose, `opik configure --use_local`) over guessing; expects the agent to verify the actual running state (containers healthy, UI responds 200, API returns the expected data) rather than assume success. Confidence: 0.6
- Reviews plans for production-resilience failure modes before implementation and sends detailed technical critiques (e.g., agent retry-loop traps, synchronous bottlenecks, fact-drift from scraped sources), each with a concrete fix and a test to prove it; expects the plan to be revised to incorporate every point and the resulting code to be test-verified for each fix (short-circuit test, async fan-out timing test, whitelist-enforcement test). Confidence: 0.9
- Prefers short-circuit/"hard stop" heuristics over unbounded agentic retry loops: an agent (e.g., extractor) must detect that data genuinely isn't in the document (JSON boolean triage) and exit immediately rather than burning retry calls and latency; hard-caps retries at 1 and expects a test asserting the early exit. Confidence: 0.9
- Wants non-dependent agents executed concurrently via `asyncio.gather` (async fan-out) rather than a synchronous linear pipeline, to cut end-to-end latency — and expects a timing test (mock sleeps) proving parallelism (~3s, not 5s). Confidence: 0.9
- Wants content-source trust enforced at the code layer, not the prompt layer: the Researcher agent gets a hardcoded domain whitelist (`irdai.gov.in`, `joinditto.in`, `beshak.org`) and non-whitelisted fetched content is silently dropped before reaching analysts; expects a test mocking an off-whitelist domain returning empty findings. Confidence: 0.9
- Wants failure paths to degrade gracefully and never block the core flow: researcher fetch failure → empty findings, unparseable triage → conservative default, missing data → supervisor prompts the user for the full document; tests must assert no crash on failures. Confidence: 0.7
- Wants the heavy pipeline to avoid redundant work: when two stages (router classification + extraction) both parse the same PDF, the duplicate Docling run was treated as a defect and fixed with an in-memory per-(path, mtime) parse cache so the file is parsed once per process. Confidence: 0.6
- Designs for arbitrary/unknown user input types: "we never know what user will give" — the pipeline must handle any document the user sends (PDF, scanned image, photo, table-heavy pages) with per-format routing (e.g., PDF → heavy parser, photo → fast vision path) rather than assuming a single input shape. Confidence: 0.7
- Wants integration plans to leverage their actual local hardware: "i have an gpu so lets plan around what is the best way" — when planning a heavy library (e.g., Docling), account for GPU/CUDA acceleration and GPU-memory lifecycle rather than defaulting to CPU-only or conservative choices. Confidence: 0.6
- Windows-specific toolchain gotchas must be handled in code: torch's inductor/dynamo JIT path requires the MSVC C++ compiler (`cl.exe`), which is absent on typical dev machines — the fix is disabling it via env (`TORCHDYNAMO_DISABLE=1`, `TORCHINDUCTOR_DISABLE=1`) so the CPU pipeline runs without a compiler, and verifying whether the installed torch build is actually CUDA-capable (a `+cpu` wheel silently ignores the GPU). Confidence: 0.7
- When a performance upgrade is costly to set up (e.g., a multi-GB CUDA torch build over a slow connection, timed-out downloads), prefers dropping it and accepting the slower CPU path rather than blocking on infrastructure — explicitly said "cpu docling is fine just do this wire the TableFormer pipeline", redirecting effort to the functional feature work instead of the acceleration work. Confidence: 0.7
- Before building custom state/infrastructure layers or committing to a framework, wants the agent to inspect what the framework/SDK actually provides and report the finding — explicitly asked to "check the sdk we are using check what options they are providing" for agent state management, covering both individual (per-agent) and combined (shared) state; and, before green-lighting the framework choice, asked "check do we have anything related to memory management in langgraph or deep agent?" to verify the built-in memory capabilities of the candidate frameworks from their official docs. The agent is expected to read the installed package's API surface / official documentation and conclude what is/isn't supported before deciding on a custom state or memory layer. Confidence: 0.7
- Wants the pipeline tested against real-world documents sourced from the web for every product category (health/term/life policies), not just hand-held fixtures — asked to "download real policies from web if available each category" so the router/extractor/analyst paths get exercised on genuine insurer documents per category. Confidence: 0.6
- When scraping for real documents stalls (email-gated or JS-driven download pages), prefers to handle the manual downloads himself ("tell me what all you need i will download for you give me the names") and expects the agent to hand over an exact, actionable list — product names per category, source sites, and selection criteria (full "policy wording/specimen policy" PDF, not brochures; URL ending in .pdf) — rather than the agent over-scraping. Confidence: 0.6
- For JS-heavy download flows that resist curl/fetch, prefers the agent to automate with a Playwright Python script that renders the page and downloads files, rather than pushing manual downloading back to the user — explicitly said "it is too hard for me... write a python script use playwright to see and download" after first offering to download manually; expects the script (installed via `uv add --dev`, chromium via `playwright install`) to be run and its outputs verified (real PDF headers, page counts) rather than just handed over. Confidence: 0.75
- When public specimen policies turn out to be blank-template documents (schedule numbers left as `<< >>` placeholders), prefers generating realistic synthetic test fixtures from the real wordings — take the authentic terms from the downloaded PDFs, inject realistic filled-in schedule values (premium, sum assured, maturity, benefit illustration), and produce fully-populated per-category policies so every analysis path (XIRR, term+SIP, health scoring) is exercised with real numbers — asked to "get the data from there, add the missing info and create data out of it"; expects the synthetic fixtures to be run end-to-end through the pipeline and verified to extract and compute correctly, not just generated. Confidence: 0.65
- Cares about proactively securing the repo (finding vulnerabilities, secret leakage, dependency CVEs) as part of project quality — asked "how to find the vulnerabilities in the repo or make this project secure"; expects a security-scanning stack (SAST, secret scanning, dependency scanning) recommended with an honest applicability assessment that skips tools irrelevant to the actual project type (e.g., web-app DAST/IaC scanners for a polling bot). When the pragmatic subset is proposed, wants it actually implemented, not just recommended — explicitly directed "add the practical subset (Gitleaks + Bandit + Trivy) to pre-commit + CI, and run a first scan to see what it finds in this repo?", i.e. wire the tools into the dev gates and run an initial scan to surface and report real findings. Confidence: 0.7
- Wants open-source tool recommendations delivered as concrete ranked lists backed by verifiable metrics — explicitly asked to "give top 5 by stars" when choosing security tools; the agent verified star counts via the GitHub API rather than unverified search snippets. Confidence: 0.55
- Wants security tooling wired into the actual dev gates, not just documented: adopted tools go into pre-commit hooks (gitleaks as a hook) and a dedicated CI job (gitleaks full-history with `fetch-depth: 0`, trivy fs scan at HIGH/CRITICAL with fail-on-findings, bandit with SARIF upload to code scanning) so enforcement happens on every commit/push. Confidence: 0.7
- Wants security scan findings actually fixed in the code, not just reported or suppressed: the bandit findings (B101 `assert_used`) were resolved by rewriting the code, and the agent chose to fix them properly rather than adding a `# nosec` ignore or bandit config exclusion. Confidence: 0.6
- Prefers explicit `raise RuntimeError(...)` invariant guards over `assert` statements in production code — asserts vanish under `python -O`, so they're not a reliable runtime check; this was the pattern chosen to fix the bandit findings. Confidence: 0.6
- For one-off heavy scans that would pollute the environment, prefers running them via Docker with the repo mounted (`docker run ... aquasec/trivy fs`) over installing the tool locally; accepts the DB-download overhead and narrows the scan (skip `.venv`/`.git`, lockfile-focused) to keep it fast. Confidence: 0.5
- Checks what actually got committed before pushing when a commit looks unexpectedly large (e.g., a `uv.lock` churn) — verifies the diff stat and reasons about whether the bulk is legitimate (locking previously floating optional deps) before pushing. Confidence: 0.6
- Prefers the agent to self-drive end-to-end testing instead of the user manually feeding test inputs each round: when the user had been re-sending the same PDF every time to test the bot, they said "instead of me sending the pdf everytime can you do it by yourself and check end to end how it is working n all. fix any issues you find" — the agent should automate the test loop itself (e.g., run the pipeline directly on a local PDF via an existing script that exercises the same code path, or drive the channel via the comm SDK), hunt down any issues, and fix them autonomously rather than bouncing the test back to the user. Reconfirmed when the agent ended a diagnosis by asking the user to re-send the PDF once more to close the loop: the user replied tersely "fix the issue then" — the agent is expected to resolve the root cause itself (clean restarts, verification instrumentation like a startup log dumping the compiled graph's node names) instead of putting the manual re-test back on the user. Confidence: 0.75
- Treats the user's factual corrections about their own input as authoritative ground truth for debugging: when the live bot told the user their complete 850KB policy PDF was "a partial document," the user reported it and said "FYI it is complete document" — the agent is expected to trust that, root-cause the pipeline (Opik spans showed extraction produced all-null fields → triage falsely short-circuited), fix the plumbing, and re-verify live, never to doubt the user's input. Confidence: 0.6
- During live end-to-end debugging, the user drives the loop with very terse status-check prompts (e.g., "check now", "now check") right after personally performing the external action (re-sending the real document to the bot); the agent is expected to independently inspect logs/traces and report current state without being asked to walk through each step. Repeated again with a bare "now check" immediately after the external action; the agent is expected to keep digging until it finds the real blocker (e.g., stale process running pre-fix code) rather than reporting a shallow "nothing new" status. Reconfirmed when, after a fresh bot restart, the user re-sent the document and tersely reported "got the response but it was same as previous check what is wrong" — a "same as previous" report is a signal to treat earlier hypotheses (e.g., stale process) as disproven and compare traces across runs to find the real root cause, not to re-verify the same conclusion. Confidence: 0.85
- When a code fix appears not to take effect in live testing, verifies whether the running process actually restarted with the new code — compares process start times against the edit time and checks for stale PIDs still holding the old code in memory — before concluding the fix failed; a bot that needs a restart to pick up a handler change is diagnosed by process age, not just by the log. Confidence: 0.55
- When a live path can't be reproduced and code inspection keeps going in circles, adds verification instrumentation at startup that dumps the actual compiled artifact (e.g., a "Graph nodes: ..." log listing the compiled graph's node names right after build) so the running system proves what it contains instead of being assumed — the startup log confirmed the live bot's graph did contain all rubric nodes, which disproved the stale-graph hypothesis and narrowed the hunt to the real cause. Confidence: 0.6
- During live debugging with a long-lived bot, kills stale/duplicate polling processes before testing and verifies process counts — explicitly treated multiple processes polling the same conversation as a suspected root cause, checked PIDs, parent/child relationships, and process start times vs. code-edit times, and used `Get-CimInstance Win32_Process` with a CommandLine filter to enumerate and clean up bot processes; also learned to distinguish a genuine duplicate-bot bug from Python's normal parent/child `-m` spawn (two PIDs, one logical bot) before over-claiming the diagnosis. Reconfirmed when a process check showed two PIDs with identical start timestamps after a restart — recognized as the normal parent/child spawn, not a duplicate, and only then concluded the bot was healthy. Confidence: 0.6
- A "missing data / ask user for the full document" fallback that fires when the document is actually complete is a defect to fix, not acceptable behavior: if extraction never ran (e.g., Telegram delivered the PDF as a URL, the handler never materialized it to a local path, so the vision path got a file:// URL it couldn't read → all-null fields → triage misfire), the fallback must be gated on genuine parsing — root-cause whether "data absent" or "extraction never ran" before trusting the short-circuit. Confidence: 0.55
- Prefers robust file-type detection for media URLs based on URL structure/content signals (path segment `file/`, presence of a bot token, upstream gateway quirks like a missing slash), not just file-extension suffix: any Telegram-file URL should be treated as a download target regardless of suffix, and malformed URLs should be normalized (e.g., `api.telegram.orgfile` → `api.telegram.org/file`) so the correct pipeline branch triggers instead of silently falling through to a broken path. Confidence: 0.6
- Wants the repo's git branching strategy grounded in industry standards, not invented ad hoc: asked "how our branching strategy should be? what industry standards say?" right after the versioning work — expects the mainstream models (GitHub Flow, Git Flow, GitLab Flow, trunk-based with release branches) laid out with honest tradeoffs and a recommendation fitted to the project's actual size (solo/hackathon → simple trunk-based flow with feature branches, no `develop` branch, releases as tags on `main`) rather than heavyweight process. Confidence: 0.6
- Cares that each tagged release stays frozen and isolated even as newer versions land on `master` — asked directly whether previous versioned work "will be overridden" when a new version comes to master; expects the isolation mechanism explained concretely (tags as immutable commit pointers, hotfix branches cut from the tag, cherry-picking fixes back to master, never force-pushing shared branches or deleting tags) with the repo's own graph shown, not just reassurance. Confidence: 0.6
- Wants workflow/engineering policies enforced as automated guardrails, not just documented: after approving the GitHub Flow branching recommendation ("i like it document it"), asked for it as "guardrails so if someone unknowingly tries to make mistake... will be able to show the correct way" — expects git hooks (pre-commit blocking direct commits to master, pre-push blocking force-pushes to master) wired via pre-commit and actually installed, where the blocked output prints the exact correct workflow (create a `feat/`/`fix/` branch and PR to master; use `git revert`/cherry-pick instead of force-push) rather than just refusing silently. The user proactively probes the guardrail set for blind spots (asked "how are you planning to avoid this what guardrails do we have?" about the `git checkout v0.1.0` → detached HEAD → divergent-history trap) and expects coverage to extend to every failure mode of the policy — including a dedicated hook that blocks commits in detached HEAD and prints the branch-first fix (`git switch -c hotfix/v0.1.x v0.1.0`). When asked "how are we making sure that this is being followed — do we have some system check?", expects enforcement to extend beyond advisory local hooks to a server-side CI check (a `git-guardrails` job that fails violating PRs: direct pushes to master, missing changelog for `src/` changes, moved release tags — unbypassable via `--no-verify`) plus remote branch protection (protected master, force-push disabled, read-only tags), framed as three layers of defense-in-depth: local hooks (advice), CI (enforcement), host rules (hard protection). Confidence: 0.8
- Guardrails/blocking hooks must be proven to work in both directions before being shipped: the guardrail was tested to fire-and-block on master, to pass on a feature branch, and to trigger on `--force` push args — with encoding failures (Windows cp1252 choking on `❌`/`✔` glyphs) fixed so the guidance actually prints. A "block direct commits to master" guardrail must also distinguish PR merges from direct pushes (merging a PR is itself a push to master): it should skip commits whose message starts with `Merge pull request #N from ...` and be verified green on the post-merge push run — a naive check silently breaks the normal GitHub Flow on every merge. Confidence: 0.6
- When the agent has `gh` access to the user's GitHub account, expects it to perform server-side repo administration itself rather than leaving it as a documented recommendation: after the agent said branch protection must be configured on GitHub and "I can't set it from here", the user directed "you have gh access so add a branch protection yourself" — the agent pushed the repo, enabled branch protection on `master` via `gh api` (required PR reviews, required `git-guardrails` status check, no force-pushes/deletions, enforce-admins) and created an active tag Ruleset making `refs/tags/v*` immutable, then verified both. Confidence: 0.8
- pre-commit quirk in this repo: the `pre-push` stage re-runs `pre-commit`-stage hooks, so a legitimate `git push origin master` triggers the `no-commit-on-master` guardrail and blocks the push; the correct exception is `git push --no-verify` when pushing an already-reviewed/merged line (the guardrail targets *committing* to master, not pushing it) — applied for both the master push and feature-branch pushes. Confidence: 0.6
- CI workflows that upload CodeQL/bandit SARIF to code scanning need the `security-events: write` permission (plus `contents: read`, and `actions: read` where appropriate) at the job level; without it the upload fails with "Resource not accessible by integration" on push events — user expects these permission gaps found and fixed when verifying CI. Confidence: 0.6
- After fixing CI config, the verification is not done until the fixed workflow is proven on the exact failing path: create a PR, watch its checks pass, merge it, then confirm the post-merge push run to master is green (this run is what exercises the previously-broken job on a `push` event). Warnings in logs (Node 20 deprecation, CodeQL v3 deprecation) are acceptable to defer. Confidence: 0.6
- When required infrastructure is missing, prefers standing it up locally via Docker to enable the true production path for live testing rather than settling for in-memory/substitute backends or deferring: after the agent caveated that the LangGraph live test ran on in-memory backends because Postgres wasn't running, the user directed "run the postgres on the local docker?" — the agent then spun up a pgvector Postgres container, wired `.env`, and re-ran the full Postgres-backed live test, which surfaced four real production bugs only visible on that path (invalid `row_factory`, missing autocommit for `CREATE INDEX CONCURRENTLY`, dict-row indexing, Windows Proactor-vs-Selector event loop) that were then fixed. Confidence: 0.8
- Per-page/two-stage LLM extraction designs must include a guardrail: the per-page triage model checks against a fixed gold-standard checklist (the guidelines become the rubric, so "concerning" = "violates a known pattern", not free-form judgment that drifts page-to-page or hallucinates from boilerplate), deterministic calculators stay the source of truth for all numbers, and the layman-writer model receives only accumulated findings + computed flags — no raw pages, no math. Confidence: 0.6
- Wants a separate prompt AND rubric per product type (HEALTH / LIFE-ULIP / TERM), not one shared generic prompt — explicitly said "for each product we should have seprate prompt and rubric"; each product's rubric is its own structured checklist with its own thresholds, severity levels, plain-language explain/action templates, and source attribution (Ditto / IRDAI / advisor framework), and the prompt builders are driven from that product's rubric. Confidence: 0.7
- Prefers to use the framework's native mechanism for storing prompts when one exists: asked the agent to verify "whether langgraph provides a way to store prompts" before designing a custom prompt layer, and locked in LangGraph's official pattern — rubrics and prompt templates stored as versioned, namespaced JSON docs in the same BaseStore/MemoryStore used for memory, seeded at startup from checked-in JSON (editable without code changes), with a fallback to the checked-in file when the store is empty. Confidence: 0.6
- When verifying whether a live feature actually worked, points the agent at the exact Opik trace URL in the local observability UI as the source of truth ("you check using this correct http://localhost:5173/...") — expects the agent to fetch and analyze that trace and its spans (actual LLM inputs, node names, timestamps) rather than rely on logs alone; this includes discovering the Opik REST API when the UI is a loopback-only SPA (web_fetch can't reach it), and reconciling what actually ran against what was claimed (e.g., which pipeline path the live message took, trace timestamps vs. bot process start) before concluding success or failure. Reconfirmed when checking the trigger-script run: "you can go here and see what was the final response http://localhost:5173/" — the UI is also the place to read the final response of a flow run, not just trace internals. Reconfirmed again when the live bot produced unexpected output: the user's next move was to paste the exact trace URL ("this is what i got http://localhost:5173/.../logs?trace=019fec47...") and expect the agent to fetch that trace's spans and reconcile what actually ran (legacy path, no `media_route` log) against process state before concluding — the trace is the arbiter of what happened. Confidence: 0.85
hat actually ran against what was claimed (e.g., which pipeline path the live message took, trace timestamps vs. bot process start) before concluding success or failure. Confidence: 0.6
- Debugging methodology for a multi-agent pipeline that misbehaves end-to-end: wants each agent/stage run individually on the real input first to see what each one produces, then the combined pipeline run and its outputs compared against intent — keep output that matches intent, fix the stage that doesn't; explicitly requested "run each agent or step then start combine the agents and see what they are producing and if it intended then keep it or fix it". Confidence: 0.7
- When a "new" pipeline path silently falls back to a legacy path despite the config flag being enabled, treats the effective path as the ground truth to prove — not the config: the rubric triage path makes ~24 LLM calls (per-page triage + table + layman) while the legacy supervisor makes ~4, so counting LLM calls / distinct log markers (e.g., "GRAPH INVOKE" vs "Media → input_path=") in the log fingerprints which path actually ran; the root cause was `GraphRuntime.start()` discarding the `_build_graph()` return value so `graph` stayed `None` and the `if langgraph_enabled and graph is not None` gate silently fell back to the supervisor. Expected fixes: capture the build result, add a regression test guarding the wiring (`tests/test_main_runtime.py`), and confirm the live bot routes correctly — not just trust the startup log that says "LangGraph enabled". Confidence: 0.65
