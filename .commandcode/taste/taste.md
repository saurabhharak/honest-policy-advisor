# Taste

## Workflow
- Prefers test-first / TDD: write test cases before implementing a new feature. Confidence: 0.95

## Tooling
- Use the `gh` CLI for GitHub operations (creating remote repos, pushing); user expects it to be authenticated and available. Confidence: 0.65
- Shell is Windows cmd.exe: do not chain commands with `;` — run them as separate shell commands. Confidence: 0.9
